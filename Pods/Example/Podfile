use_frameworks!

target 'TACCircularProgress_Example' do
  pod 'TACCircularProgress', :path => '../'

  target 'TACCircularProgress_Tests' do
    inherit! :search_paths

    pod 'FBSnapshotTestCase' , '~> 2.1.4'
  end
end
